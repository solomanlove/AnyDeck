# AdbManage

AdbManage 是一个轻量级 Flutter Desktop 工具箱，面向 Android 开发和 QA 调试流程。

## 作者信息

| 项目 | 内容 |
|---|---|
| 作者 | shijie |
| 定位 | Android 开发调试工具维护者 |
| 软件方向 | 聚焦 ADB、scrcpy 和 Android QA 调试流程的轻量桌面工具 |

## 软件说明书

### 软件定位

AdbManage 基于 Flutter Desktop 构建，把常用 Android 调试能力集中到一个桌面端工具中，适合开发、自测、QA 排查和设备状态检查。

### 使用前准备

| 依赖 | 说明 |
|---|---|
| `adb` | 必需，用于设备发现、Shell、应用管理、文件传输和日志读取 |
| `scrcpy` | 可选，用于外部投屏 |
| Android 设备 | USB 调试或无线调试需要在手机端开启开发者选项 |

### 基础使用流程

1. 通过 USB 连接设备，或使用 TCP/IP、二维码、配对码完成无线连接。
2. 在设备列表中选择目标设备。
3. 按需进入概览、控制、应用、文件、Logcat、终端或 `scrcpy` 功能面板。
4. 执行清除数据、卸载、冻结、重启等影响设备状态的操作前，先确认目标设备和包名。

首版实现遵循 `FLUTTER_ADB_DESKTOP_TECH_PLAN.md`：

```text
Flutter Desktop UI
  -> Dart Process 调用 adb
  -> Dart Process 启动外部 scrcpy
  -> Riverpod 管理设备、会话和长生命周期进程
```


## 更新日志

### v0.12.0 — 2026-05-29（本次提交）

本次提交重点重构主面板展现层目录，按照功能进行合理分包，以降低单目录文件膨胀程度：

1. **Dashboard Presentation 结构重构**
   - 根目录下原本堆积的 36 个 Dart 文件精简为 **1 个** 主入口文件 (`dashboard_screen.dart`)。
   - 其余 35 个文件按照业务及功能特性，分别归类到 11 个子目录中：`apps/`、`control/`、`devices/`、`files/`、`layout/`、`logcat/`、`overview/`、`processes/`、`screenshot/`、`terminal/`、`webpages/` 和 `widgets/`。
2. **Part / Part of 与 Import 链修复**
   - 自动完成了 23 个 Part 文件的 `part of '../dashboard_screen.dart';` 路径指向升级。
   - 修复了被移动的 8 个独立 Tab 文件的相对 `import` 路径前缀（例如将引用核心库及本地化的相对路径升级为 `../../../../`）。
   - 主入口中对独立 Tab 文件的 `import` 路径同步添加了子包层级。
3. **消除分析器警告 (Analyzer Warning)**
   - 修复了主入口中 `effectiveSelectedDevice!` 的冗余非空断言（`unnecessary_non_null_assertion`）警告，使项目在 `flutter analyze` 静态分析中实现 **Zero Issues**。

---

### v0.11.0 — 2026-05-28


1. **布局分析核心服务拆分**
   - 新增 `LayoutInspectorService`，集中负责执行 `uiautomator dump`、读取 XML、通过 `exec-out screencap -p` 抓取截图。
   - 新增 `LayoutInspectorSnapshot`，一次性返回布局树、原始 XML 和截图字节。
   - 新增 `LayoutInspectorException`，在布局抓取失败时保留 `AdbResult`，便于 UI 同步设备断连状态。
2. **布局节点模型独立**
   - 新增 `LayoutNode`，承载 `uiautomator` 节点属性、父子关系、布尔属性和 `bounds` 坐标解析。
   - 将 `parseLayoutXml` 和 XML entity 反转义逻辑从 UI 文件迁移到 core 层，供树、属性表、截图预览和测试复用。
   - `bounds` 解析支持负坐标，兼容被裁剪或越界的 Android View 节点。
3. **Layout Tab 抓取流程重构**
   - `LayoutTab` 改为通过 `layoutInspectorServiceProvider` 获取布局快照，UI 不再直接拼接 ADB 命令和 `Process.run`。
   - 切换设备时会清空旧布局内容并重置旋转、缩放、展开状态。
   - 刷新新截图时会释放旧 `ui.Image`，减少桌面端长时间抓取布局时的图片资源占用。
   - 布局抓取失败且识别到设备断连时，会调用 `syncAfterAdbResult` 同步设备注册表和选中设备状态。
4. **布局分析 UI 拆分**
   - 新增 `LayoutToolbar`，把刷新、保存、复制 XML、展开/折叠、旋转、缩放、显示属性、显示边框等工具栏控制从 `layout_tab.dart` 拆出。
   - 层级树、属性表和屏幕预览统一依赖 core 层 `LayoutNode`，减少 UI 文件之间的互相引用。
   - 层级树自动滚动前增加 `RenderObject` 存活检查，避免节点已卸载时调用 `ensureVisible`。
5. **布局预览与概览细节优化**
   - 屏幕预览继续支持点击截图选中最深层节点，并按当前旋转角度映射回设备原生坐标。
   - 概览 Header 改为独立 `_OverviewHeader`，窄宽度下上下排列并横向滚动快捷按钮，降低 overflow 风险。
   - 设备 Header 简化 scrcpy 区域，仅保留启动投屏和状态展示，降低顶部区域复杂度。
6. **单元测试补充**
   - 新增 `layout_node_test.dart`，覆盖 XML 树解析、属性反转义和负坐标 `bounds` 解析。

---

### v0.10.0 — 2026-05-28

本次提交主要围绕终端调试收藏命令、本地化文案和工作台细节做结构整理，并同步记录风险与影响范围：

1. **终端收藏命令模块拆分**
   - 将 `FavoriteCommand` 和 `FavoriteCommandsNotifier` 从 `adb_terminal_session.dart` 拆分到 `favorite_commands.dart`。
   - 影响范围：终端收藏命令的默认命令、用户自定义命令、删除默认命令记录和 `SharedPreferences` 持久化读取。
   - 风险点：旧版本已保存的收藏命令仍依赖原有 JSON 字段，当前保留兼容字段；如果偏好数据被手动破坏，异常会被忽略并回退到默认命令。
2. **终端状态文案本地化**
   - `TerminalLine` 新增 `l10nKey` 和参数支持，终端初始化、进程退出、启动失败、离线发送、重连状态等系统文案改为运行时按语言渲染。
   - 终端标签名从状态字段改为 UI 根据顺序生成，例如 `终端 1` / `Terminal 1`。
   - 影响范围：终端多标签展示、终端输出行渲染、重启终端、Ctrl+C、清屏等控制按钮 tooltip。
   - 风险点：如果新增的 localization key 缺失，会直接影响终端系统提示文本；普通 adb stdout / stderr 输出不受影响。
3. **收藏命令 UI 文案补齐**
   - 重置默认命令确认、暂无收藏命令、填入、执行、删除收藏等文案全部改为 localization。
   - 默认收藏命令标题改为 `titleKey`，支持中英文切换时同步展示当前语言。
   - 影响范围：终端右侧常用命令面板，以及用户新增、删除、重置收藏命令流程。
   - 风险点：自定义收藏命令仍使用用户输入文本，不参与翻译；这是预期行为。
4. **工作台与概览细节调整**
   - 左侧 Rail 禁用态图标显式使用灰色，避免不可用工具入口和可点击入口视觉混淆。
   - 工具内容卡片移除重复标题栏，减少页面纵向占用。
   - 概览页标题、快捷操作和刷新按钮恢复为同一行左右布局。
   - 影响范围：主页 / 控制 / 应用 / 文件 / Logcat / 终端 / 进程管理 / 网页调试等工具页顶部视觉结构。
   - 风险点：概览页 Header 对极窄宽度仍敏感，需要重点回归窗口宽度较小时是否出现 overflow。
5. **总体回归关注点**
   - 终端创建、关闭、重连、发送命令、Ctrl+C 和清屏。
   - 收藏命令新增、删除默认命令、删除自定义命令、重置默认命令、切换中英文后的默认命令标题展示。
   - 窄窗口下左侧 Rail、概览页 Header 和终端收藏命令面板的布局表现。

---

### v0.9.0 — 2026-05-28

本次提交重点修复桌面工作台在窄窗口和低高度下的布局溢出问题，并补充响应式测试：

1. **桌面工作台响应式布局修正**
   - 主界面统一走左侧 Rail + 内容区布局，窄窗口下不再切回无 Rail 的紧凑布局，保证导航入口始终可见。
   - 左侧 Rail 按可用高度计算可展示工具数量，优先保留前置高频入口，并把末尾工具逐项收进**更多功能**菜单。
   - `更多功能` 菜单只展示当前被收纳的工具，选中被收纳工具时菜单图标同步高亮。
2. **设备 Header 防溢出**
   - 设备顶部 Header 改为基于 `LayoutBuilder` 自适应排版。
   - 极窄宽度下设备图标、名称、scrcpy 控制、状态和关闭按钮会横向滚动，避免按钮和标题互相挤压。
3. **概览页防溢出**
   - 概览标题、快捷操作和刷新按钮改为横向滚动布局，窄窗口下不会溢出。
   - 概览信息卡片在宽度极小时会压缩图标并隐藏标题文本，优先保证布局稳定。
4. **工具标题栏防溢出**
   - 工具页标题栏支持横向滚动，避免窗口过窄时图标和标题造成 overflow。
5. **文案与规则调整**
   - 将导航中的 `概览 / Overview` 调整为 `主页 / Home`，更贴近左侧 Rail 的首页入口语义。
   - `AGENTS.md` 增加 Agent 入口说明，并补充“数据、逻辑、样式分离”和“单个类文件不超过 500 行”的项目约束。
6. **Widget 测试补强**
   - 新增多组桌面外壳 widget 测试，覆盖高窗口完整展示、窄宽度保留 Rail、低高度逐项收纳、更多菜单高亮，以及极窄宽度下概览卡片不溢出。

---

### v0.8.0 — 2026-05-28

本次提交继续优化桌面工作台的设备选择、导航收纳和概览快捷操作：

1. **设备自动选中与返回首页**
   - 启动或刷新后如果当前未选中设备，会自动选择第一台在线设备，减少进入工具页前的手动点击。
   - 点击左侧 Logo 会主动清空当前设备选择并返回设备首页，同时记录用户手动清空状态，避免马上又被自动选中。
2. **左侧工具栏自适应收纳**
   - 左右宽度变化时始终保留左侧 Rail，避免窄窗口下导航入口突然消失。
   - 左侧 Rail 在高度不足时按视觉顺序逐项收纳工具按钮，优先保留概览、控制、应用等高频入口。
   - `更多功能` 菜单承载当前被收纳的末尾入口，窗口越低收纳越多，避免小窗口下按钮溢出。
   - 补齐 `moreTools` 中英文 localization 文案。
3. **设备显示信息优化**
   - 设备列表的连接展示改为优先显示设备型号和序列号，例如 `Pixel 8(abc123)`，便于区分同型号或多连接设备。
   - `RegisteredDevice` 新增 `serial` 字段，合并 USB / 无线设备时保留真实序列号用于展示。
4. **概览页快捷操作**
   - 设备概览标题栏右侧新增常用快捷按钮：Home、Back、Power、通知栏展开和当前焦点窗口查询。
   - 这些操作会根据设备在线状态自动启用或禁用，避免对离线设备发起无效 ADB 命令。
5. **紧凑布局优化**
   - 小窗口布局下模拟器管理面板支持折叠 / 展开，避免设备列表和模拟器列表同时抢占纵向空间。
   - 设备工作区 Header 的投屏控制区域改为横向可滚动，减少 scrcpy 会话较多时的按钮挤压。
6. **开发工具与测试同步**
   - 新增 `devtools_options.yaml`，启用 `shared_preferences` DevTools 扩展配置。
   - 更新拼音过滤测试入口查找方式，从文本查找切换为应用图标查找，适配新的 Rail 导航结构。

---

### v0.7.0 — 2026-05-28

本次提交重点新增了进程管理、Web/WebView 调试、投屏窗口置顶选项，并对部分设备概览与控制功能进行了优化与清理：

1. **进程管理功能**
   - 界面上新增**“进程管理”**标签页，支持以表格形式展示当前设备上正在运行的进程，包含进程名、PID、% CPU、CPU 时间、用户等指标。
   - 支持实时过滤与模糊搜索，提供**“仅显示应用”**开关，方便用户从系统进程中快速筛选出第三方应用进程。
   - 提供了**“结束进程”**操作，可对指定进程进行终止，并伴有二次确认弹窗和明确的成败结果提示。

2. **Web 与 WebView 网页调试**
   - 新增**“网页调试”**标签页，能够自动扫描并列出目标设备上当前处于活跃状态的 Web 和 WebView 页面，展示网页 Title、URL、类型（Page/WebView）以及所属的应用包名。
   - 提供了**“调试网页”**（基于 Chrome DevTools）和**“浏览器打开”**的快捷操作，支持对列表中网页的过滤和检索。

3. **投屏窗口置顶功能 (scrcpy)**
   - 在 scrcpy 投屏控制面板中，新增**“投屏窗口保持最前”**选项，支持动态传参至 scrcpy，从而确保启动投屏后的窗口能保持悬浮在最前端，提升多窗口协同调试的体验。

4. **设备概览与控制优化**
   - **Android ID 移至概览**：将原本位于控制面板的 Android ID 显示移至“设备概览”面板，使其与设备基础硬件和系统属性聚合在一起。
   - **Wi-Fi 状态动态检测与开关**：控制面板的 Wi-Fi 按钮升级为动态检测，可显示当前的 Wi-Fi 开/关状态，并执行对应的 adb 命令来开启/关闭设备 Wi-Fi，保持 UI 和设备真实状态同步。
   - **“版本”按钮清理**：彻底清理移除了设备管理面板中已无实际用途的“版本”控制按钮及相关前后端代码逻辑。

5. **本地化与单元测试支持**
   - 新增了所有相关新功能（进程管理、网页调试、投屏窗口置顶等）的中英文翻译，维护了良好的国际化支持。
   - 补充并更新了与新模块相关的单元测试，确保代码的健壮性。

---

### v0.6.0 — 2026-05-27

本次提交重点增强 ADB 断连感知与离线设备信息查看能力：

1. **ADB 断连自动检测与设备状态同步**
   - `AdbResult` 新增设备断连检测逻辑，通过正则匹配 `device 'xxx' not found` 错误信息自动识别设备是否已断开。
   - 所有 ADB 操作（`_runAdbAction` / `_showAdbResult`）执行完成后，若检测到设备断连，会自动刷新设备列表并同步选中状态，避免 UI 显示过时的设备信息。
   - 断连设备的提示信息统一展示为中文"adb已断开"，替代原始英文错误输出。
2. **离线设备手机信息概览缓存**
   - 新增 `cachedDeviceOverviewProvider`，离线设备可从本地持久化缓存读取手机信息概览，无需设备在线也能查看品牌、型号、内存、分辨率等硬件信息。
   - 设备在线时使用实时 Stream 数据源，离线时自动切换为本地缓存数据源，切换逻辑对用户透明。
   - 若本地无缓存数据，展示"您没有保存过这个手机信息"友好提示。
3. **中英文本地化文案补齐**
   - 新增 `loadingCachedOverview`（正在读取本地缓存）和 `noCachedOverview`（您没有保存过这个手机信息）的中英文 localization 字符串。
4. **单元测试补充**
   - 新增 ADB 设备断连消息解析的单元测试，覆盖 `isDeviceDisconnected`、`disconnectedDeviceId` 和断连消息展示场景。

---

### v0.5.0 — 2026-05-27

本次提交重点优化桌面端主界面、品牌资源和设备管理稳定性：

1. **桌面端工作台布局重构**
   - 大屏模式下主界面改为左侧 Rail + 二级 Sidebar + 右侧内容区的桌面工作台布局。
   - 左侧 Rail 集中放置设备首页、应用、文件、Logcat、终端、ADB 重启和设置入口。
   - 右侧工具内容区取消传统 `TabBarView`，改为独立工具页标题栏和内部滚动，减少桌面端滚轮抢占和布局跳动。
2. **首页与设备侧栏优化**
   - 未选择设备时显示设备列表和模拟器管理首页，便于直接完成设备连接、刷新和模拟器启动。
   - 二级 Sidebar 展示当前设备、设备工具入口和 scrcpy 状态，选中设备后可快速切换常用调试模块。
3. **设备批量删除**
   - 设备列表新增**删除已选设备**按钮，支持一次性删除多台勾选设备。
   - 删除无线设备时会先执行断开连接，并同步清理历史记录、别名和序列号映射。
   - 补齐批量删除相关中文和英文 localization 文案。
4. **ADB 设备刷新与 mDNS 解析增强**
   - 新增主动刷新设备列表逻辑，刷新后立即同步到设备注册表，不再只依赖 Provider 重建。
   - `adb devices -l` 解析改为正则匹配，支持包含空格的 mDNS 设备 ID，例如 `adb-xxx (2)._adb-tls-connect._tcp`。
   - 新增 `adb_service_test.dart` 单元测试覆盖 mDNS 设备 ID 解析场景。
5. **品牌 Logo 与应用图标**
   - 新增 `assets/brand/app_logo.svg` 和 `assets/brand/app_logo.png` 品牌资源，并在主界面 Rail 中展示。
   - 更新 macOS AppIcon 和 Windows `app_icon.ico`，统一桌面端应用图标。
   - 新增 `tool/generate_app_icons.swift`，用于从同一套绘制逻辑生成 macOS、Windows 和应用内 Logo 资源。
6. **提示样式优化**
   - 全局提示从 `ScaffoldMessenger SnackBar` 调整为 Overlay 浮层，避免受页面结构影响，并统一顶部展示位置。

---

### v0.4.0 — 2026-05-27

本次提交补充了设置说明、应用管理和交互反馈能力：

1. **设置页说明入口**
   - 设置弹窗新增**作者信息**入口，集中展示作者、定位和工具维护方向。
   - 设置弹窗新增**软件说明书**入口，说明软件定位、依赖准备、基础使用流程和高风险操作注意事项。
2. **应用冻结与解冻**
   - 应用管理操作栏新增**冻结 / 解冻**按钮，根据应用当前启用状态自动切换操作。
   - 冻结使用 `pm disable-user --user 0 <package>`，无需 root 权限；解冻使用 `pm enable <package>`。
   - 操作成功后自动刷新应用列表，避免 UI 状态与设备真实状态不一致。
3. **交互反馈优化**
   - 刷新设备后新增**设备已刷新**提示，明确反馈用户操作结果。
   - 全局 SnackBar 改为浮层样式，区分成功和失败图标，提升桌面端提示可读性。
   - ADB 命令默认完成 / 失败文案改为中文，减少界面中英文混杂。
4. **中英文文案补齐**
   - 补齐作者信息、软件说明书、设备刷新、应用冻结 / 解冻相关的中文和英文 localization 文案。

---

### v0.3.0 — 2026-05-27

本次更新新增了多项设备管理与调试增强功能：

1. **设备去重与连接方式显示**
   - 对同一物理设备通过 USB 和无线同时连接的场景进行**自动去重**，基于硬件序列号（`ro.serialno`）合并同一设备的多条连接记录。
   - 设备列表中新增**连接方式标识**（USB / 无线 / mDNS），清晰展示每台设备的当前连接途径。
2. **重启 ADB 服务**
   - 设备管理工具栏新增**"重启 ADB 服务"**按钮，可一键执行 `adb kill-server` + `adb start-server` 完成服务重启。
3. **二维码无线配对连接**
   - 新增**"无线调试配对"**功能入口，支持两种配对方式：
     - **二维码配对**：自动生成配对二维码，手机端在"开发者选项 → 无线调试 → 使用二维码配对设备"中扫描即可完成配对。
     - **配对码配对**：手动输入配对端口和配对码进行配对连接。
4. **模拟器管理**
   - 新增**"模拟器管理"**面板，自动扫描本地已创建的 Android AVD 模拟器。
   - 支持查看模拟器**运行状态**（已停止 / 启动中 / 运行中），并提供**启动**和**停止**操作按钮。
   - 运行中和启动中的模拟器自动**置顶排序**，方便快速操作。
5. **终端调试**
   - 新增**"终端调试"**标签页，为每台设备提供完整的 ADB Shell 交互终端。
   - 支持创建**多个终端实例**（多标签页），可独立关闭和删除。
   - 内置**命令历史记录**，可通过上下方向键快速翻阅历史命令。
   - 支持**常用命令收藏**，可自定义添加、删除和管理收藏命令，点击即可快速执行。
   - 支持**一键重置默认收藏命令**，恢复内置的常用命令集合。
   - 提供**清屏**、**Ctrl+C 中断**、**重启终端**等快捷操作按钮。
6. **设备控制增强**
   - 新增**菜单键**模拟（`KEYCODE_MENU`），方便调试旧版 Android 应用。
   - 新增**通知栏展开**控制（`cmd statusbar expand-notifications`），一键下拉设备通知面板。
   - 新增**屏幕自动旋转**开关，可远程切换设备的 `accelerometer_rotation` 设置。
7. **屏幕信息增强**
   - 设备信息概览中新增**屏幕逻辑密度**显示，自动将 DPI 换算为倍率并标注密度级别（如 `3x (xxhdpi)`）。
   - 新增**屏幕刷新率**显示，通过解析 `dumpsys display` 获取设备支持的刷新率信息。
   - 修复了设备信息卡片底部**"分辨率"和"字体缩放"显示不全**的问题。

---

### v0.2.0 — 2026-05-27（上一次更新）

1. **应用全面汉化**
   - 将全平台（macOS、Windows、Linux、Flutter 应用）显示名称统一重命名为**"安卓手机管理"**。
   - 丰富了 UI 中文本地化字符串，确保界面文案完全汉化且符合中文使用习惯。
2. **隐藏侧边栏与顶部状态显示**
   - 在选中设备后，自动隐藏左侧边栏以释放更多内容空间。
   - 顶部导航栏动态显示当前选中的设备名称。
3. **设备管理 UI 重构**
   - 重新设计了设备管理界面，移除原先无设备选中时的空白占位状态面板。
   - 引入支持排序的设备信息表格，可展示设备标识、自定义名称、连接状态等。
   - 支持编辑修改设备昵称/别名。
   - 支持一键重新连接、断开连接及删除离线设备记录。
4. **设备信息本地持久化缓存**
   - 成功加载的设备硬件及系统属性（品牌、型号、内存、分辨率等）会被自动写入本地持久化缓存。
   - 当设备离线或再次切换加载时，能够实现零延迟即时展示。
5. **拼音支持应用过滤搜索**
   - 应用管理列表的搜索框不仅支持中文和包名搜索，还全面支持**拼音首字母**和**全拼**的过滤匹配（例如输入"wx"或"weixin"即可快速筛选出"微信"）。
6. **详细应用信息弹窗展示**
   - 点击应用列表中任一应用，可打开"应用信息"详细对话框。
   - 支持查看：包名、版本、是否系统应用、最小 SDK、目标 SDK、首次安装时间、最后更新时间、安装包大小。
   - 支持动态加载获取应用大小、数据大小及缓存大小（通过 `dumpsys diskstats` 和 `dumpsys package` 等命令解析）。
   - 支持显示并一键复制**签名 MD5 校验码**。
7. **应用安装包（APK）导出**
   - 列表操作栏新增**"导出安装包"**按钮，可一键通过 `adb pull` 将设备上已安装的 APK 安装包导出并保存到本地电脑。

## 当前范围

| 模块 | 状态 | 说明 |
|---|---:|---|
| 设备列表 | ✅ 已实现 | 轮询 `adb devices -l`，自动去重、型号/序列号展示、批量删除 |
| TCP/IP 连接 | ✅ 已实现 | `adb connect`、二维码配对、配对码配对 |
| ADB 服务管理 | ✅ 已实现 | 一键重启 ADB 服务端 |
| scrcpy 启动器 | ✅ 已实现 | 使用默认 MVP 参数启动外部 `scrcpy` |
| 设备操作 | ✅ 已实现 | 文本输入、Home/Back/Power/Menu 键、音量、Wi-Fi、通知栏、屏幕旋转、当前焦点查询 |
| 设备信息 | ✅ 已实现 | 硬件概览含逻辑密度和刷新率，支持本地持久化缓存 |
| 布局辅助 | ✅ 已实现 | 布局边界、深色/浅色模式、Layout Inspector XML 树与截图预览 |
| 应用管理 | ✅ 已实现 | 安装 APK、应用列表（拼音搜索）、启动、强停、清数据、冻结/解冻、卸载、详情查看、APK 导出 |
| 进程管理 | ✅ 已实现 | 获取设备进程列表（PID/CPU/CPU 时间/用户）、支持仅显示应用、搜索与杀死进程 |
| 网页调试 | ✅ 已实现 | 扫描并列出运行中的 Web/WebView 页面、支持在浏览器中打开或调试网页 |
| 文件管理 | ✅ 已实现 | 浏览 `/sdcard/`、拖拽上传、下载、删除 |
| Logcat | ✅ 已实现 | 启停日志流、保留最近 1000 行、关键字筛选 |
| 终端调试 | ✅ 已实现 | 多标签 ADB Shell、命令历史、常用命令收藏与重置 |
| 模拟器管理 | ✅ 已实现 | AVD 列表扫描、启动/停止模拟器、运行状态监控、紧凑布局折叠展示 |
| 品牌资源 | ✅ 已实现 | 应用内 Logo、macOS AppIcon、Windows ICO 图标生成与更新 |

拖拽行为：

| 拖入文件 | 动作 |
|---|---|
| `.apk` | `adb install -r <apk>` |
| 其他文件 | `adb push <file> <current remote path>` |

## 项目结构

```text
lib/
  app/
    l10n/              # 中文本地化字符串
    router/
    theme/
  core/
    adb/               # ADB 服务封装
    apps/              # 应用管理服务
    device_actions/    # 设备操作（按键、旋转、通知栏等）
    device_info/       # 设备信息采集与持久化
    emulator/          # Android 模拟器管理服务
    files/             # 文件管理服务
    logcat/            # Logcat 日志服务
    process/           # 进程管理服务
    providers/         # Riverpod 全局状态管理
    scrcpy/            # scrcpy 投屏服务
    terminal/          # ADB 终端会话管理
    web_debug/         # Web 与 WebView 调试服务
  features/
    dashboard/
      presentation/    # 主界面展现层入口 (dashboard_screen.dart)
        apps/          # 应用管理模块
        control/       # 控制中心模块
        devices/       # 设备发现、连接、配对与模拟器
        files/         # 手机文件管理器
        layout/        # 界面布局分析 (Layout Inspector)
        logcat/        # Logcat 日志查看器
        overview/      # 主界面外壳与概览页
        processes/     # 进程管理器
        screenshot/    # 屏幕截图与录制
        terminal/      # 交互式终端 (ADB Shell)
        webpages/      # 手机网页调试
        widgets/       # 公共对话框与通用组件
assets/
  brand/               # 应用 Logo 品牌资源
tool/
  generate_app_icons.swift
test/
  adb_service_test.dart
  web_debug_test.dart
```

## 开发命令

```bash
flutter pub get
flutter analyze
flutter test
```

本地手动检查运行命令：

```bash
flutter run -d macos
```

## 本地依赖

| 工具 | 用途 |
|---|---|
| `adb` | 设备发现和 shell 命令 |
| `scrcpy` | 外部投屏能力 |
| Flutter Desktop | macOS、Windows、Linux 桌面目标 |

## 代码约定

| 类型 | 约定 |
|---|---|
| 代码注释 | 使用中文描述意图、边界和非显然逻辑 |
| 文档 | 使用中文编写，必要的 API、CLI、类名和协议名保留英文 |
| UI 文案 | 优先走本地化字符串表，避免在 Widget 中散落硬编码文案 |

ADB 和 scrcpy 路径自定义能力后续放入设置模块。
